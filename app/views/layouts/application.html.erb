<!DOCTYPE html>
<html>
<head>
  <title>MyGists</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Gist Agregator", "/", :class => "navbar-brand" %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <li class="<%= 'active' if current_page?(gists_path) || current_page?(root_path) %>">
          <%= link_to gists_path do %>
              <i class="icon-list"></i> All gists
          <% end %>
        </li>
        <li class="<%= 'active' if current_page?(new_gist_path) %>">
          <%= link_to new_gist_path do %>
              <i class="icon-plus"></i> Create New
          <% end %>
        </li>
      </ul>
      <form class="navbar-form navbar-left pull-right" role="search" action="/gists/search">
        <div class="form-group">
          <%= select :lang, Pygments.lexers.keys.sort.map! {|l| l.downcase.strip}, {}, class: "form-control" %>
        </div>
        <button type="submit" class="btn btn-default">Search</button>
      </form>
    </div>
  </div>
</div>
<div class="row">
    <div class="container">
        <%= yield %>
    </div>
</div>
</body>
</html>
